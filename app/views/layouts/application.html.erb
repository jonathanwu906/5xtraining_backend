<!DOCTYPE html>
<html>
  <head>
    <title>Task Manager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <header>
      <nav class="bg-blue-600 p-4 text-white">
        <div class="container mx-auto flex justify-between items-center">
          <%= link_to 'Task Manager', root_path, class: 'text-lg font-bold' %>
          <div class="flex space-x-4">
            <% if logged_in? %>
              <% if current_user.admin? %>
                <%= link_to '管理使用者', admin_users_path, class: 'bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded mr-4' %>
              <% end %>
              <%= link_to '所有任務', tasks_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-4' %>
              <%= link_to '登出', logout_path, data: { turbo_method: :delete, turbo_confirm: '你確定要登出嗎？' }, class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
            <% else %>
              <%= link_to '註冊', signup_path, class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mr-4' %>
              <%= link_to '登入', login_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main class="container mx-auto mt-4 max-w-4xl">
      <% flash.each do |key, message| %>
        <div class="mb-4 p-4 rounded <%= key == 'success' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
